# ----------------------------------------
# SMART DATABASE CONFIGURATION
# ----------------------------------------

# 1. DATABASE URL
# Render will automatically fill in ${DB_URL}.
# If it's missing (on your laptop), it uses localhost.
spring.datasource.url=${DB_URL:jdbc:mysql://avnadmin:AVNS_xr2OkP3dVZB1zNvwgQu@mysql-11f4a0b-adityasharma21850-fd18.g.aivencloud.com:25661/defaultdb?ssl-mode=REQUIRED}

# 2. USERNAME
# Render will fill in ${DB_USERNAME}.
# On laptop, it uses 'root'.
spring.datasource.username=${DB_USERNAME:root}

# 3. PASSWORD
# Render will fill in ${DB_PASSWORD}.
# On laptop, it uses 'Arigato@20'.
spring.datasource.password=${DB_PASSWORD:Arigato@20}

# ----------------------------------------
# JPA SETTINGS
# ----------------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
