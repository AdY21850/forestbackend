# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/forest_db
spring.datasource.username=root
spring.datasource.password=Arigato@20

# Spring Data JPA Configuration
# This tells Hibernate (part of JPA) to auto-update your database tables
spring.jpa.hibernate.ddl-auto=update

# This is helpful for debugging, as it will print the SQL queries
spring.jpa.show-sql=true