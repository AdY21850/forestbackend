# ==========================================
# 1. DATABASE CONNECTION
# ==========================================
spring.datasource.url=jdbc:mysql://avnadmin:AVNS_z0Gan6FbBY89LLAphkt@mdb-adityasharma21850-fd18.j.aivencloud.com:25661/defaultdb?ssl-mode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_z0Gan6FbBY89LLAphkt


# ==========================================
# 2. JPA / HIBERNATE
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.defer-datasource-initialization=true


# ==========================================
# 3. ZOHO SMTP (PRODUCTION-SAFE)
# ==========================================
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

# --- SMTP AUTH ---
spring.mail.properties.mail.smtp.auth=true

# --- STARTTLS (MANDATORY FOR PORT 587) ---
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# --- TRUST ZOHO CERT ---
spring.mail.properties.mail.smtp.ssl.trust=smtp.zoho.in

# --- DEBUG (REMOVE AFTER CONFIRMATION) ---
spring.mail.properties.mail.debug=true
